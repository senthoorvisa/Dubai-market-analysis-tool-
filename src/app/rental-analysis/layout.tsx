import '../globals.css';

export const metadata = {
  title: 'Dubai Rental Analysis | Real-time Rental Data',
  description: 'Find current rental prices and analyze the Dubai rental market with real-time data',
};

export default function RentalAnalysisLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 